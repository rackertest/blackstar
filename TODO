At the time of this writing, this program compiles fine with no optimization. 
If optimization is used it will work with the -f option, but single host options will segfault. 
Also need configure file to execute without having to chmod +x it after download. 
Lastly, the man page should be installed without the user having to copy the man file.
